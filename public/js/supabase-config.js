/*
 * Supabase credentials for the quiz + admin pages.
 *
 * These point at the bingo-dash project. The publishable key is safe
 * to ship to the browser — RLS policies in supabase/schema.sql
 * (admin allowlist via the `admin_users` table) enforce access.
 */
window.SUPABASE_URL = 'https://hzpqohfnszlxchjswvtc.supabase.co';
window.SUPABASE_ANON_KEY = 'sb_publishable_cuIOy1l1Cud__ESQtqax0g_bRfpbwuW';
